import {
  OrderStatus,
  PaymentMethod,
  ReferralStatus,
  UserRole,
  WithdrawStatus,
  type backendInterface,
} from "../backend";

export const mockBackend: backendInterface = {
  approvePayment: async () => true,
  approveWithdrawal: async () => true,
  assignRole: async () => ({ __kind__: "ok", ok: null }),
  checkLoginLockout: async () => ({ locked: false, remainingSecs: BigInt(0) }),
  clearLoginAttempts: async () => undefined,
  createOrder: async (items) => ({
    id: BigInt(1),
    status: OrderStatus.Pending,
    total: 1500,
    userId: "user-1" as any,
    date: BigInt(Date.now()),
    items,
  }),
  createReferralCode: async () => "REF-DEMO123",
  generateOtp: async () => "482951",
  getAdminStats: async () => ({
    newOrders: BigInt(5),
    pendingPayments: BigInt(3),
    pendingWithdrawals: BigInt(2),
  }),
  getAllOrders: async () => [
    {
      id: BigInt(1),
      status: OrderStatus.Pending,
      total: 1500,
      userId: "user-1" as any,
      date: BigInt(Date.now()),
      items: [{ qty: BigInt(2), name: "Premium T-Shirt", price: 750 }],
    },
    {
      id: BigInt(2),
      status: OrderStatus.Delivered,
      total: 3200,
      userId: "user-2" as any,
      date: BigInt(Date.now() - 86400000),
      items: [{ qty: BigInt(1), name: "Leather Shoes", price: 3200 }],
    },
  ],
  getAllSellerLimits: async () => [["seller-1", 10000], ["seller-2", 5000]],
  getAllSellerSuspensions: async () => [],
  getSuspensionAuditTrail: async () => [],
  getReferralLeaderboard: async () => [
    { referrerId: "user-1", totalReferrals: BigInt(5), totalRewardEarned: 1000, rank: BigInt(1), lastReferralDate: BigInt(Date.now() - 86400000) },
    { referrerId: "user-2", totalReferrals: BigInt(3), totalRewardEarned: 600, rank: BigInt(2), lastReferralDate: BigInt(Date.now() - 172800000) },
  ],
  getSellerWithdrawalLimit: async () => null,
  setSellerWithdrawalLimit: async () => undefined,
  getAuditLog: async () => [
    {
      id: "audit-1",
      actionType: "APPROVE_PAYMENT",
      actorId: "admin-1",
      resourceType: "Payment",
      resourceId: "pay-1",
      details: "Payment approved for order #1",
      timestamp: BigInt(Date.now()),
      oldValue: "Pending",
      newValue: "Approved",
    },
    {
      id: "audit-2",
      actionType: "SUSPEND_USER",
      actorId: "admin-1",
      resourceType: "User",
      resourceId: "user-3",
      details: "User suspended due to fraud",
      timestamp: BigInt(Date.now() - 3600000),
      oldValue: undefined,
      newValue: undefined,
    },
  ],
  getAuditLogByAction: async () => [],
  getAuditLogByActor: async () => [],
  getAuditLogByDateRange: async () => [],
  getCommissionByMonth: async () => [
    ["Jan", 500],
    ["Feb", 700],
    ["Mar", 620],
    ["Apr", 900],
  ],
  getCommissionByPeriod: async () => [
    ["Week 1", 250],
    ["Week 2", 400],
    ["Week 3", 320],
    ["Week 4", 380],
  ],
  getCommissionSummary: async () => ({
    totalCommission: 4800,
    topProducts: [["Premium T-Shirt", 1200], ["Leather Shoes", 900]],
    topSellers: [["seller-1" as any, 2000], ["seller-2" as any, 1500]],
    byPeriod: [["Jan", 500], ["Feb", 700]],
  }),
  getFeaturedIds: async () => ["prod-1", "prod-2"],
  getFeaturedIdsByCategory: async () => ["prod-1", "prod-2"],
  getMyEarningsByPeriod: async () => [
    { period: "Jan", grossRevenue: 5000, netRevenue: 4500, commissionDeducted: 500 },
    { period: "Feb", grossRevenue: 7000, netRevenue: 6300, commissionDeducted: 700 },
  ],
  getMyEarningsSummary: async () => ({
    totalNetEarnings: 28000,
    remainingBalance: 12000,
    totalWithdrawn: 16000,
    periodLabel: "Last 30 days",
    totalGrossRevenue: 32000,
    totalCommissionDeducted: 3200,
  }),
  getMyOrders: async () => [
    {
      id: BigInt(1),
      status: OrderStatus.Delivered,
      total: 1500,
      userId: "user-1" as any,
      date: BigInt(Date.now()),
      items: [{ qty: BigInt(1), name: "Premium T-Shirt", price: 1500 }],
    },
  ],
  getMyPrincipal: async () => "2vxsx-fae",
  getMyProductStats: async () => [
    {
      productId: "prod-1",
      productName: "Premium T-Shirt",
      netRevenue: 13500,
      salesCount: BigInt(18),
      totalRevenue: 15000,
      commissionDeducted: 1500,
    },
    {
      productId: "prod-2",
      productName: "Leather Shoes",
      netRevenue: 8100,
      salesCount: BigInt(9),
      totalRevenue: 9000,
      commissionDeducted: 900,
    },
  ],
  getMyReferralEarnings: async () => BigInt(500),
  getMyReferrals: async () => [
    {
      id: "ref-1",
      status: ReferralStatus.Completed,
      completedAt: BigInt(Date.now() - 86400000),
      referrerReward: BigInt(200),
      code: "REF-DEMO123",
      refereeId: "user-5",
      refereeBonus: BigInt(100),
      referrerId: "user-1",
    },
  ],
  getMyRole: async () => UserRole.Customer,
  getMySellerOrders: async () => [
    {
      id: BigInt(3),
      status: OrderStatus.Ongoing,
      total: 2400,
      userId: "user-4" as any,
      date: BigInt(Date.now()),
      items: [{ qty: BigInt(3), name: "Casual Shirt", price: 800 }],
    },
  ],
  getPayment: async () => null,
  getReferralCode: async () => "REF-DEMO123",
  getSellerWallet: async () => null,
  getTopProducts: async () => [["Premium T-Shirt", 1200], ["Leather Shoes", 900]],
  getTopSellers: async () => [["seller-1" as any, 2000], ["seller-2" as any, 1500]],
  getUserProfile: async () => ({ displayName: "Demo User" }),
  getUserRole: async () => UserRole.Customer,
  isAdminSetup: async () => true,
  listAllPayments: async () => [
    {
      id: BigInt(1),
      status: "Approved" as any,
      method: PaymentMethod.bKash,
      sellerEarnings: 1350,
      userId: "user-1" as any,
      submittedAt: BigInt(Date.now() - 3600000),
      commission: 150,
      orderId: BigInt(1),
      amount: 1500,
      transactionId: "TXN123456789",
      reviewedAt: BigInt(Date.now()),
    },
  ],
  listAllWithdrawals: async () => [
    {
      id: BigInt(1),
      status: WithdrawStatus.Pending,
      method: PaymentMethod.bKash,
      sellerId: "seller-1" as any,
      accountNumber: "01712345678",
      amount: 5000,
      requestedAt: BigInt(Date.now() - 7200000),
      reviewedAt: undefined,
    },
  ],
  listCommissions: async () => [],
  listPendingPayments: async () => [],
  listPendingWithdrawals: async () => [],
  myPayments: async () => [],
  myWallet: async () => ({
    pendingWithdrawal: 0,
    totalWithdrawn: 16000,
    totalEarnings: 32000,
    sellerId: "seller-1" as any,
  }),
  myWithdrawals: async () => [],
  processReferral: async () => true,
  recordLoginFailure: async () => BigInt(1),
  rejectPayment: async () => false,
  rejectWithdrawal: async () => false,
  requestAdminRole: async () => ({ __kind__: "ok", ok: "Request submitted" }),
  requestWithdrawal: async () => BigInt(1),
  resetAdminPassword: async () => ({ __kind__: "ok", ok: null }),
  setUserProfile: async () => undefined,
  setupAdminPassword: async () => ({ __kind__: "ok", ok: null }),
  submitPayment: async () => BigInt(1),
  updateFeaturedIds: async () => ({ __kind__: "ok", ok: null }),
  updateOrderStatus: async () => ({ __kind__: "ok", ok: null }),
  verifyAdminPassword: async () => true,
  verifyOtp: async () => true,
};
